{
  "importMap": "imports.json",
  "tasks": {
    "install": "deno install --allow-read --allow-run --unstable -n ndpkg mod.ts",
    "review": "deno lint && deno fmt --check",
    "format": "deno fmt"
  },
  "fmt": {
    "options": {
      "indentWidth": 2,
      "lineWidth": 110,
      "singleQuote": true,
      "useTabs": false
    }
  }
}
